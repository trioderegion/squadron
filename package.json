{
  "name": "squadron",
  "version": "1.0.0",
  "description": "fvtt module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rollup --config node:rollup-config-badger-den --config-den src/squadron:dev",
    "build": "rollup --config node:rollup-config-badger-den --config-den src/squadron:release"
  },
  "devDependencies": {
    "rollup-config-badger-den": "^1.3.0"
  }
}
